const elemento = document.querySelector('.miClaseCss')

elemento.setAttribute('type', 'text') // por ejemplo nosotros ahora estamos cambiando el tipo de un input

atributo = elemento.getAttribute('type')

atributo = elemento.removeAttribute('type')
url = 'https://robertoorazi.com.ar'
let ventanaAbierta = window.open() // Abre el link en una ventana
let ventanaCerrada = window.close() // Cierra la ventana en caso de estar abierta
let ventanaEstaCerrada = window.closed // Nos dice si esta cerrado
let ventanaNoEjecuta = window.stop() // Deja de ejecutar la ventana
ventana.stop() // Deja de ejecutar la ventana
ventana.closed // Nos dice si esta cerrado

window.alert('HOLA') // Estos son los alerts que salen de arriba\
window.print() // esto es para imprimir la pantalla
window.prompt('dame la guita') // esto es un input en un alert
window.confirm('Estas seguro que queres salir del sitio') // Esto es un alert con 2 botones, 1 confirmar 1 cancelar.


